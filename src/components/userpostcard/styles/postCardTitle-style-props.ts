const imageStyleProps = {
  maxW: "40px",
  minH: "40px",
  w: "40px",
  h: "40px",
  alt: "views",
  border: "3px solid orange",
  borderRadius: "full",
};

const flexContainerStyleProps = {
  direction: "row" as "row",
  p: "2",
  justify: "center",
  align: "center",
};

const postCardContainerStyleProps = {
  display: "flex",
  direction: "column" as "column",
};

export {
  imageStyleProps,
  postCardContainerStyleProps,
  flexContainerStyleProps,
};
