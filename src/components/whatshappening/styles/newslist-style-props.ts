const newsListContainerStyleProps = {
  as: "section" as "section",
  direction: "column" as "column",
  gap: "5",
};

const textStyleProps = {
  fontWeight: "semibold",
  fontSize: "sm",
};

const boxStyleProps = {
  varient: "ghost",
  cursor: "pointer",
};
const headingStyleProps = {
  as: "h4" as "h4",
  size: "md",
  fontWeight: "bold",
};

const textdescriptionStyleProps = {
  fontWeight: "medium",
  fontSize: "sm",
  mt: "1",
};

export {
  newsListContainerStyleProps,
  textStyleProps,
  headingStyleProps,
  boxStyleProps,
  textdescriptionStyleProps
};
