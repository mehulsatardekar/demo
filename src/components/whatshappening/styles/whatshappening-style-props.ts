const headingStyleProps = {
  as: "h1" as "h1",
  size: "md",
  fontStyle: "Inter",
};

const boxStyleProps = {
  as: "section" as "section",
  position: "sticky" as "sticky",
  top: "20",
  borderRadius: "10px",
  boxShadow: "base",
};
export { headingStyleProps , boxStyleProps};
